// 1. Dynamic Shop Status (Open 8am - 8pm)
//function updateShopStatus() {
  //  const statusElem = document.getElementById('status-bubble');
    //const now = new Date();
    //const hour = now.getHours();
  
    //if (hour >= 8 && hour < 20) {
      //statusElem.textContent = "● Open Now - Come in for a brew!";
      //statusElem.style.color = "#81c784"; // Light green
    //} else {
      //statusElem.textContent = "○ Closed - Opening at 8:00 AM";
      //statusElem.style.color = "#e57373"; // Light red
   // }
  //}
  
  // 2. Handle Contact Form Submission
//  const contactForm = document.querySelector("#contactForm");
 // const submitBtn = document.querySelector("#submitBtn");
//  
//  contactForm.addEventListener("submit", function (e) {
  //  e.preventDefault();
    
    // Visual feedback
  // const originalText = submitBtn.textContent;
   // submitBtn.textContent = "Sending...";
  ///  submitBtn.disabled = true;
  
    // Simulate a network request
   // setTimeout(() => {
   //   alert("Thanks for contacting Brew Haven! We'll get back to you soon ☕");
     // contactForm.reset();
     // submitBtn.textContent = originalText;
     // submitBtn.disabled = false;
 //   }, 1200);
  //});
  
  // Initialize
//  document.addEventListener("DOMContentLoaded", () => {
 //   updateShopStatus();
//  });
